#!/usr/bin/env bash

vm_stat | awk '
  NR == 1 { page_size = $8 }
  /^Pages active:/ { active = $3 }
  /^Pages wired down:/ { wired = $4 }
  /^Pages occupied by compressor:/ { compressed = $5 }
  END { printf "%3.0fGB\n", (active + wired + compressed) * page_size / 1024^3 }
'
